Vue.component('name', {
  data: function () {
  },
  template: 
})
new Vue({el:})

组件是可复用的VUE实例
data必须是个函数
因此每个实例可以维护一份被返回对象的独立的拷贝
否则将会调用一次影响到其他实例
组件命名：全小写包含连字符
局部注册出现：减少用户下载的 JavaScript 的无谓的增加（在组件后给定参数）


